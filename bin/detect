#!/usr/bin/env bash
# bin/detect <build-dir>

BUILD_DIR=$1

echo "Starphleet-mono-buildpack - Running DETECT. build directory: " ${BUILD_DIR}

cd ${BUILD_DIR}

if [ -f "$( find . -maxdepth 2 -iname *.sln )" ]; then
  echo ".NET" && cd - && exit 0
else
  echo "no" && cd - && exit 1
fi
