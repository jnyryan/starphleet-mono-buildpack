#!/usr/bin/env bash
# bin/release <build-dir>
set -e

echo "Starphleet-mono-buildpack -  Running RELEASE. build directory is :" ${BUILD_DIR}

cat << EOF
---
default_process_types:
  web: echo "RELEASED"
EOF
