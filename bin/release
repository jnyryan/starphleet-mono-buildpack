#!/usr/bin/env bash
# bin/release <build-dir>
set -e

echo "Starphleet-mono-buildpack -  Releasing from build directory is :" ${BUILD_DIR}

cat << EOF
---
default_process_types:
  web: curl -X POST -d "Released BuildPax" http://requestb.in/usywgrus
EOF
