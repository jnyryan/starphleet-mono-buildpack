#!/usr/bin/env bash
# bin/release <build-dir>
set -e

logger "Starphleet-mono-buildpack -  Releasing from build directory is :" ${BUILD_DIR}

cat << EOF
---
default_process_types:
  web: bash start.sh
EOF
